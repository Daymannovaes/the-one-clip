version: '3'

services:
  media-tools:
    build: .
    volumes:
      - ./input:/app/input
      - ./output:/app/output
    environment:
      - FFMPEG_PATH=/usr/bin/ffmpeg
      - FFPROBE_PATH=/usr/bin/ffprobe 
